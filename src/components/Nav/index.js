import React from 'react'
import './nav.styles.scss';

const Nav = () => {
    return (
        <header className="Nav">
            <h1 className="Nav-title">JSON Factory</h1>
        </header>
    );
}

export default Nav;